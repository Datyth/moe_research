# Fast 1-epoch sanity check for the Phase B MoE enhancement stage.
extends: isic2018_moe.yaml

experiment:
  name: phase_b_isic2018_moe_smoke

scheduler:
  name: none

training:
  epochs: 1
  batch_size: 4
  num_workers: 4
  gradient_clip_norm: null
