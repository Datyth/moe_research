# E1: MoE-SAM — SAM ViT-B + Adapter fine-tuning + Sparse MoE routing.
extends: isic2018_common.yaml

experiment:
  name: isic2018_e1
  output_root: runs

model:
  name: esam
  image_size: 256
  checkpoint: checkpoints/sam_vit_b_01ec64.pth
  moe_num_experts: 4
  moe_top_k_ratio: 0.5
  freeze_backbone: true
