# E0: SAM + Adapter, no MoE (same as isic2018_e1.yaml but use_moe: false).
extends: isic2018_common.yaml

experiment:
  name: isic2018_e0
  output_root: runs

model:
  name: esam
  image_size: 256
  checkpoint: checkpoints/sam_vit_b_01ec64.pth
  use_moe: false
  freeze_backbone: true
