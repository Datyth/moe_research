extends: ../b6_distill_deploy_100ep.yaml

experiment:
  name: c2_mean_distill
  output_root: runs/phase_c_loss_ablations

task:
  name: phase_c_distill
  latent_objective: mean_mse
  lambda_latent: 1.0
  lambda_route: 1.0
  lambda_deploy: 1.0
